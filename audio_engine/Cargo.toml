[package]
name = "meditation_engine"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"] # Critical: Compiles to .wasm, not a system binary

[dependencies]
wasm-bindgen = "0.2"
js-sys = "0.3" 
console_error_panic_hook = "0.1"
audio_core = { path = "../../audio_core" }
